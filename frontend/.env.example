# Frontend env example (Vite build-time)
#
# Vite only exposes variables that start with `VITE_` to the browser.
# Docker builds the production bundle during `npm run build`, so this
# should be present before building the frontend image if you change it.

VITE_API_BASE_URL=http://localhost:8080

